//            01234567891011
const nome = 'Wesley Icaro'

let alunos = ['Luiz', 'Maria', 'João'] // Luana, Victor
alunos = 544

console.log(typeof alunos) // Vai passar a informação que é um object

console.log(alunos instanceof Array) // Se dar "True" é realmente um array

// console.log(alunos.slice(0, -2)) // Do começo até o antepenúltimo

// console.log(alunos.slice(0, 3)) // Mostra os 3 primeiros do array

// delete alunos[1] // remove o conteúdo do indíce indicado, mas mantém a existência do indíce sem  alterar os demais.

// alunos.unshift('Lidio') // Adiciona no inicio
// alunos.unshift('Tamara') // Adiciona no inicio

// const removidos = alunos.pop() // remove do final

// const removidos = alunos.shift() // remove do começo

// console.log(removidos)

// alunos.push('Lidio') // Adiciona no fim
// alunos.push('Tamara') // Adiciona no fim

// alunos[0] = 'Gabriel' // Altera
// alunos[3] = 'Gabriel' // Adiciona

// alunos[alunos.length] = 'Guilhermino' // Adiciona no fim
// alunos[alunos.length] = 'Vagner' // Adiciona no fim
// alunos[alunos.length] = 'Lulu' // Adiciona no fim
// alunos[alunos.length] = 'VDelas' // Adiciona no fim
